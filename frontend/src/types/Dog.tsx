type Dog = {
  name: string;
  weight: number;
  sex: "samiec" | "samiczka";
  age: number;
  desciription: string;
  imgSrc: string;
  state: string;
  shelter: Shelter;
  id: string;
  description: string;
};

// TODO: state to enum
